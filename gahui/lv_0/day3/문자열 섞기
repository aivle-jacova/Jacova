// charAt 메서드는 문자열에서 특정 위치(인덱스)의 문자를 쉽게 추출 가능

class Solution {
    public String solution(String str1, String str2) {
        String answer = "";
        for(int i=0; i<str1.length(); i++){
            answer = answer+str1.charAt(i)+str2.charAt(i);
        }
        return answer;
    }
}