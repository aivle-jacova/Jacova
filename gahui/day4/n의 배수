class Solution {
    public int solution(int num, int n) {
        int answer = 0;
        

        if(num % n == 0){ // 0이 된다는건 배수기 때문에 몫이 안 남아서 % 사용.
            return 1;
        }
        else {
             return 0; 
        }
    }
}